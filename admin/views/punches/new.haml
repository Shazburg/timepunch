.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:punches, :index)
      %li.active=link_to pat(:new), url(:punches, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:punch)
    .inner
      -form_for :punch, url(:punches, :create), :class => :form do |f|
        =partial "punches/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")