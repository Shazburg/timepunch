.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:employees, :index)
      %li.active=link_to pat(:new), url(:employees, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:employee)
    .inner
      -form_for :employee, url(:employees, :create), :class => :form do |f|
        =partial "employees/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")